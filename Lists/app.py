letters = ['a','b','c']
numbers = [1,2,3]
hybrid = [letters,numbers]

#suppose we want to add 100 zeros to a list , we won't want to do that manually 
zero = [0] * 100

hybrid2 = letters + numbers

listCustom = list(range(20))

chars = list("Hello, world")
print(chars)